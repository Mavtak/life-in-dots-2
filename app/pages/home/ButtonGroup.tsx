import styled from 'styled-components';

const ButtonGroup = styled.div`
  display: flex;
  flex-direction: row;
  gap: 10px;
  margin-right: auto;
  margin-left: auto;
`;

export default ButtonGroup;
